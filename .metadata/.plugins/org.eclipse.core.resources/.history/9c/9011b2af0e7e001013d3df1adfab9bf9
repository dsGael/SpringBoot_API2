package com.com.formaciondbi.springboot.app.usuarios.controllers;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import com.com.formaciondbi.springboot.app.usuarios.models.entity.Usuario;
import com.com.formaciondbi.springboot.app.usuarios.models.service.IProductoService;

@RestController
public class UsuarioController {
	
	@Autowired
	private IProductoService productoService;
	
	@GetMapping("/listar")
	public List<Usuario> listar(){
		return productoService.findAll();
	}
	
	@GetMapping("/listar/{id}")
	public Usuario detalle(@PathVariable Long id) {
		return productoService.findById(id);
	}

}